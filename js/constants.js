export const CELL_SIZE = 8; //in px
export const COLS = Math.ceil(window.innerWidth / CELL_SIZE);
export const ROWS = Math.ceil(window.innerHeight / CELL_SIZE);

export const period = 700 //time in ms between 2 redraws